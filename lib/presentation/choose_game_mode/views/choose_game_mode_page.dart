import 'package:flutter/material.dart';

class ChooseGameModePage extends StatelessWidget {
  const ChooseGameModePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ChooseGameModePage'),
      ),
      body: Container(),
    );
  }
}
